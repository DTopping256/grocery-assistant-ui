import React from 'react';
import './App.css';

/**
 * The component representing the entire single page application, which is rendered inside the root element of the project template.
 */
export default class App extends React.Component {
	public render () {
		return <div className="App"></div>;
	}
}
